import 'package:flutter/material.dart';

/// App color palette for EduSync application
class AppColors {
  // Primary brand colors
  static const primary = Color(0xFF4776E6);
  static const primaryLight = Color(0xFF6A8FF7);
  static const primaryDark = Color(0xFF2857C7);

  // Secondary brand colors
  static const secondary = Color(0xFF8E54E9);
  static const secondaryLight = Color(0xFFA77BEE);
  static const secondaryDark = Color(0xFF6C35C7);

  // Gradient colors
  static const gradientStart = Color(0xFF4776E6);
  static const gradientEnd = Color(0xFF8E54E9);

  // Text colors
  static const textDark = Color(0xFF212121);
  static const textMedium = Color(0xFF666666);
  static const textLight = Color(0xFF9E9E9E);
  static const textWhite = Colors.white;

  // Background colors
  static const background = Colors.white;
  static const backgroundLight = Color(0xFFF5F5F5);
  static const backgroundDark = Color(0xFFE0E0E0);

  // Status colors
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFE53935);
  static const warning = Color(0xFFFFB300);
  static const info = Color(0xFF2196F3);

  // Other utility colors
  static const divider = Color(0xFFE0E0E0);
  static const overlay = Color(0x80000000);
}
